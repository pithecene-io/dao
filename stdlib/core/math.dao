fn abs(x: i32): i32
  if x < 0:
    return 0 - x
  return x

fn min(a: i32, b: i32): i32
  if a < b:
    return a
  return b

fn max(a: i32, b: i32): i32
  if a > b:
    return a
  return b

fn clamp(x: i32, lo: i32, hi: i32): i32
  if x < lo:
    return lo
  if x > hi:
    return hi
  return x
