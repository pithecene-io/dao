fn identity<T>(x: T): T -> x

fn first<T, U>(a: T, b: U): T -> a

fn pick<T>(a: T, b: T, flag: bool): T
    if flag:
        return a
    return b

class Pair<A, B>:
    left: A
    right: B

type IntPair = Pair<i32, i32>
