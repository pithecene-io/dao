module probe::pipelines
type List = i32

fn filter(pred: i32): i32 -> 0
fn map(f: i32): i32 -> 0

fn positive_squares(xs: List<i32>): List<i32>
    return xs
    |> filter |x| -> x > 0
    |> map |x| -> x * x
